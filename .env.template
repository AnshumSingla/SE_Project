# ===================================
# ESSENTIAL .env VARIABLES FOR YOUR SYSTEM
# ===================================

# ==========================================
# 🤖 AI/LLM Configuration (OPTIONAL)
# ==========================================
# OpenAI API key for enhanced email classification
# If not provided, system will use rule-based classification (works fine!)
OPENAI_API_KEY=your_openai_api_key_here_or_leave_empty

# ==========================================  
# 🔐 Google OAuth Configuration (REQUIRED for production)
# ==========================================
# Get these from Google Cloud Console for your app
GOOGLE_CLIENT_ID=your_google_oauth_client_id
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret

# ==========================================
# 🗃️ File Paths (OPTIONAL - for demo/development)
# ==========================================
# Only needed if testing with file-based credentials instead of OAuth
GMAIL_CREDENTIALS_FILE=credentials.json
GMAIL_TOKEN_FILE=token.json
CALENDAR_CREDENTIALS_FILE=calendar_credentials.json  
CALENDAR_TOKEN_FILE=calendar_token.json

# ==========================================
# ⚙️ Application Settings (OPTIONAL)
# ==========================================
# Flask server configuration
PORT=5000
FLASK_DEBUG=false

# Email processing limits
MAX_EMAILS_TO_PROCESS=50
DAYS_BACK_TO_SCAN=7

# Default timezone for calendar events
DEFAULT_TIMEZONE=America/New_York

# ==========================================
# 📊 Database (OPTIONAL - defaults work fine)
# ==========================================
# SQLite database path (auto-created if not exists)
DATABASE_PATH=users.db

# Encryption key file (auto-generated if not exists)  
ENCRYPTION_KEY_FILE=encryption.key